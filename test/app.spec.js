define(['react', 'backbone'], function(React, Backbone) {

  var TestUtils;

  describe('Header', function() {

    beforeEach(function() {
      TestUtils = React.addons.TestUtils;
    });

    it("should have 3 links", function () {

    });

//    it("Check Text Assignment", function () {
//      var label = React.createElement('label', {}, "Some Text We Need for Test");
//      TestUtils.renderIntoDocument(label);
//      //expect(label.refs.p).toBeDefined();
//      expect("hello").toBe("hello");
//      //expect(label.refs.p.props.children).toBe("Some Text We Need for Test")
//    });
//
//    iit("should have 3 links", function () {
//      var user = new Backbone.Model({displayName: "User Name"});
//      var header = Header({user: user});
//      var headerElement = TestUtils.renderIntoDocument(header);
//      var dropDown = TestUtils.findRenderedDOMComponentWithClass(headerElement, "dropdown-menu");
//      var listItems = TestUtils.scryRenderedDOMComponentsWithTag(dropDown, "li");
//      expect(listItems.length).toBe(3);
//    });

  });

});
