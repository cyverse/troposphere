/** @jsx React.DOM */

define(
  [
    'react'
  ],
  function (React) {

    return React.createClass({

      render: function () {
        return (
          <div>
            You can add instances to this project during launch by selecting this project from the drop-down.
          </div>
        );
      }

    });

  });
