/** @jsx React.DOM */

define(
  [
    'react',
    'backbone'
  ],
  function (React, Backbone) {

    return React.createClass({

      propTypes: {

      },

      render: function () {
        return (
          <span><a href="#">{"?iPlant Cloud - Tucson?"}</a></span>
        );
      }

    });

  });
