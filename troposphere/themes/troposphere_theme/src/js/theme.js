import theme from 'cyverse-ui/styles/cyverseTheme';

const { 
    palette: {
        primary1Color,
        accent1Color
    }
} = theme;

const THEME = {
    mainHeader: {
        background: primary1Color,
        border: "blue"
    }
};
